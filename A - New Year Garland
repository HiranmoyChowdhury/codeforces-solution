     #include<bits/stdc++.h>
    #define bst           ios_base::sync_with_stdio(0);
    #define bug(x)        cerr<<#x<<'='<<x<<endl;
    #define lp(i,a,b)     for(int i=a; i<=b; i++)
    #define ll            long long int
    #define pb            emplace_back
    using namespace std;
    int main(){
        ll t, i, j, k;
        cin>>t;
        while(t--){
            ll arr[3];
            bool f=false;
            cin>>arr[0]>>arr[1]>>arr[2];
            sort(arr, arr+3);
            if(arr[2]>arr[1]+arr[0]+1) puts("No");
            else puts("Yes");
        }
        
        
        
        
        
    	
        return 0;
    }
